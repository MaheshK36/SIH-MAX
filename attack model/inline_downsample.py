import pandas as pd; import os; import sys; os.chdir(r'C:\Users\mahes\Downloads\attack model'); df = pd.read_csv('data/archive_combined.csv'); df = df.sample(n=50000, random_state=42).reset_index(drop=True); df['src_ip'] = ['10.0.' + str(i//5000) + '.1' for i in range(len(df))]; df['dst_ip'] = '10.0.0.254'; df.to_csv('data/archive_combined.csv', index=False); with open('downsample_done.txt', 'w') as f: f.write(f"OK: {len(df)} rows, {df['src_ip'].nunique()} groups\n"); print("DONE")
